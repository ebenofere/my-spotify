import { styled } from "../../styled-system/jsx";

export const Pills = styled("div", {
    base: {
        w: 'full',
        borderRadius: '20px',
        padding: "5px 3px",
        fontSize: "12px",
        fontWeight:"bold",
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    }
});